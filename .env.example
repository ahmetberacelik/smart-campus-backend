# =====================================================
# Smart Campus Backend - Environment Variables
# =====================================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun:
# cp .env.example .env
# =====================================================

# -----------------------------------------------------
# Database Configuration
# -----------------------------------------------------
# Lokal geliştirme: 138.68.99.35 (sunucu IP)
# Production (Docker): smart_campus_db (container name)
DB_HOST=138.68.99.35
DB_PORT=3306
DB_NAME=smart_campus
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

# -----------------------------------------------------
# JWT Configuration
# -----------------------------------------------------
# Secret: Minimum 256-bit (64 hex karakter) güvenli bir key
# Oluşturmak için: openssl rand -hex 32
JWT_SECRET=your_super_secret_jwt_key_minimum_256_bits_long_here
JWT_ACCESS_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000

# -----------------------------------------------------
# Email Configuration (Gmail SMTP)
# -----------------------------------------------------
# Gmail App Password gereklidir (normal şifre çalışmaz)
# App Password oluşturmak için: Google Account > Security > 2FA > App Passwords
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password

# -----------------------------------------------------
# DigitalOcean Spaces Configuration
# -----------------------------------------------------
# S3 uyumlu object storage - Profil fotoğrafları için
# Spaces Dashboard'dan Access Key oluşturun
DO_SPACES_KEY=your_spaces_access_key
DO_SPACES_SECRET=your_spaces_secret_key
DO_SPACES_ENDPOINT=https://fra1.digitaloceanspaces.com
DO_SPACES_BUCKET=smart-campus
DO_SPACES_REGION=fra1

# -----------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------
# CORS ve email linklerinde kullanılacak frontend URL
FRONTEND_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------------------------------
# Service Discovery (API Gateway -> Services)
# -----------------------------------------------------
# Lokal geliştirme: localhost
# Production (Docker): auth-service (container name)
AUTH_SERVICE_HOST=localhost
AUTH_SERVICE_PORT=8081